

// passport.serializeUser((user: User, done) => {
//   done(null, user.id);
// });

// passport.deserializeUser(async (id: string, done) => {
//   try {
//     const user = await findOrCreateUser({ id, name: '', email: '' });
//     done(null, user);
//   } catch (error) {
//     done(error);
//   }
// });
      

